# URL do back-end Fastify
# Deixe vazio para usar o mock local (padrão de desenvolvimento)
VITE_API_URL=https://cha-back-end.onrender.com

# Defina como "false" para usar a API real em vez do mock
# Deixe como "true" (ou não defina) para usar dados simulados em memória
VITE_USE_MOCK=false
